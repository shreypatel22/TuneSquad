import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';

// react routers
// define diff routes
// each route will be for a component
  // they will be like anchor tags


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);